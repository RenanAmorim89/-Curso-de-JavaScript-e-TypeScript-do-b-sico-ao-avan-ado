// String, number, undefined, null, boolean
const nome = "Renan"; //string
const nome1 = "Renan"; //string
const nome2 = `Renan`; //string
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefinid -> não aponta para local nenhum na memoria
const sobrenomeAluno = null; // Nulo -> não aponta para local nenhum na memoria
const aprovado = false; //Boolean = true , false (logico)
